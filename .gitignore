__pycache__/
*.pyc
.loop-state.json
.venv/
loop/.state.json

# Generated packets. Regenerated on every `onboard`, so tracking the working
# copy means a conflict on every pull. The audit-trail story is unaffected:
# `approve` force-adds the specific packet a human signed off on.
packets/*.md
services/mock_okta/*.tmp

# secrets and generated state - never commit
.env
loop/journal.md

# Session scratch written for Daniel, not for the repo. Never committed.
SESSION-HANDOFF.md
BRANCH-DIFF.md
